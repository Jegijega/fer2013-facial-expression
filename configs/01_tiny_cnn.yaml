# first real CNN, 2 conv blocks
model: tiny_cnn
run_name: 01_tiny_cnn
group: from_scratch
notes: "2 conv blocks, no regularization. Low-capacity sanity model."
tags: [cnn, small]

batch_size: 128
augment: false
epochs: 30
lr: 0.001
optimizer: adam
weight_decay: 0.0
scheduler: none
seed: 42
